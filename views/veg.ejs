<!DOCTYPE html>
<html>
<head>
    <title>Veg Recipes</title>
    <link rel="stylesheet" type="text/css" href="./stylesheets/veg.css">
</head>
<body>
    <header>
        <h1>Veg Recipes</h1>
        <nav>
            <form id="searchForm" action="/search/veg" method="GET">
                <input type="text" id="searchInput" name="srch" placeholder="Search Veg Recipes">
                <button type="submit">Search</button>
            </form>
        </nav>
        <a href="/index" class="back-to-home">Back to Home</a>
    </header>

    <main>
        <section class="recipe-container" id="recipeContainer">
            <% if (veg && veg.length > 0) { %>
                <% veg.forEach(recipe => { %>
                    <article class="recipe" onmouseover="expandRecipe(this)" onmouseout="shrinkRecipe(this)" onclick="viewRecipe('<%= recipe.RecipeName %>')">
                        <h2><%= recipe.recipename %></h2>
                        <p><strong>Ingredients:</strong> <%= recipe.ingredients %></p>
                        <p><strong>Prep Time:</strong> <%= recipe.prepTimeInMins %> mins</p>
                        <p><strong>Cook Time:</strong> <%= recipe.cookTimeInMins %> mins</p>
                        <p><strong>Total Time:</strong> <%= recipe.totalTimeInMins %> mins</p>
                        <p><strong>Servings:</strong> <%= recipe.servings %></p>
                        <p><strong>Instructions:</strong> <%= recipe.instructions %></p>
                    </article>                    
                <% }); %>
            <% } else { %>
                <p>No veg recipes found.</p>
            <% } %>
        </section>
    </main>

    <footer>
        <p>&copy; 2023 Veg Recipes. All rights reserved.</p>
    </footer>

    <script src="./javascripts/veg1.js"></script>
</body>
</html>
